<?xml version='1.0' encoding='utf-8'?>
<odoo>
    <template id="external_layout_mis">
        <div t-attf-class="header o_company_#{company.id}_layout" t-att-style="report_header_style">
            <div class="row justify-content-center align-items-center">
                <div class="col-2"/>
                <div class="col-1">
                    <img t-if="company.logo" t-att-src="image_data_uri(company.logo)" style="max-height: 100px;" alt="Logo" />
                </div>
                <div class="col-7 text-center">
                    <ul class="list-unstyled">
                        <li t-if="company.is_company_details_empty">
                            <strong>
                                <span class="h3 text-success">
                                    <t t-esc="company.partner_id" t-options='{"widget": "contact", "fields": ["name"], "no_marker": true}'/>
                                </span>
                                <t t-esc="company.partner_id" t-options='{"widget": "contact", "fields": ["address"], "no_marker": true}'/>
                            </strong>
                        </li>
                    </ul>
                </div>
            </div>
            <div class="mt-2 border-top h-1 border-bottom border-muted"/>
        </div>


        <div t-attf-class="o_company_#{company.id}_layout article o_report_layout_striped {{  'o_report_layout_background' if company.layout_background in ['Geometric', 'Custom']  else  '' }}" t-attf-style="background-image: url({{ 'data:image/png;base64,%s' % company.layout_background_image.decode('utf-8') if company.layout_background_image and company.layout_background == 'Custom' else '/base/static/img/bg_background_template.jpg' if company.layout_background == 'Geometric' else ''}});" t-att-data-oe-model="o and o._name" t-att-data-oe-id="o and o.id" t-att-data-oe-lang="o and o.env.context.get('lang')">
            <!-- This div ensures that the address is not cropped by the header. -->
            <t t-call="mis_document_layout.mis_address_layout"/>
            <t t-out="0"/>
        </div>

        <div t-attf-class="o_company_#{company.id}_layout footer o_boxed_footer">
            <div class="text-center">
                <ul class="list-inline">
                    <div t-field="company.report_footer"/>
                </ul>
                <div t-if="report_type == 'pdf'" class="text-muted">
                    Page:
                    <span class="page"/>
                    of
                    <span class="topage"/>
                </div>
                <div t-if="report_type == 'pdf' and display_name_in_footer" class="text-muted">
                    <span t-field="o.name"/>
                </div>
            </div>
        </div>

    </template>

    <template id="mis_document_basic_info">
        <t t-if="doc_basic_info">
            <div class="row">
                <t t-out="doc_basic_info" />
            </div>
        </t>
    </template>

    <template id="mis_address_layout">
        <t t-set="colclass" t-value="('col-sm-4' if report_type == 'html' else 'col-4')"/>
        <t t-if="address">
            <div class="address row">
                <div name="address" t-att-class="colclass">
                    <strong>
                        <t t-esc="address"/>
                    </strong>
                </div>
                <div class="d-flex justify-content-end col-8">
                    <t t-if="information_block">
                        <div name="information_block">
                            <t t-out="information_block" />
                        </div>
                    </t>
                </div>
            </div>
        </t>
    </template>
</odoo>
