<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="mis_purchase_order_view_only_own_record" model="ir.rule">
            <field name="name">User can only see their own created purchase orders</field>
            <field name="model_id" ref="purchase.model_purchase_order"/>
            <field name="domain_force">[('create_uid','=',user.id)]</field>
            <field name="groups" eval="[(4, ref('purchase.group_purchase_user'))]"/>
        </record>

        <record id="mis_purchase_order_manager" model="ir.rule">
            <field name="name">User can see all purchase orders</field>
            <field name="model_id" ref="purchase.model_purchase_order"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="groups" eval="[(4, ref('purchase.group_purchase_manager'))]" />
        </record>
    </data>
</odoo>
